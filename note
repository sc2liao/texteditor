# build and run:
1.cmake -S . -B build
2.cmake --build build
3. ./build/texteditor

# gdb:
1. Clear out the old configuration
rm -rf build

2. Configure with Debug symbols enabled
cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug

3. Build the project
cmake --build build